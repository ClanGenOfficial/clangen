# Schemas

These JSON schema files are auto-generated from Pydantic models. Do not edit them manually.

To regenerate, run:
```sh
uv run dump_model_schemas
```

If adding a new schema entirely, add reference to the new schema to `dump_model_schemas.py`.  Then link it to its associated files in `.vscode/settings.json`.
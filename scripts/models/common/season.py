from __future__ import annotations

from enum import Enum


class Season(Enum):
    greenleaf = "greenleaf"
    leaf_fall = "leaf-fall"
    leaf_bare = "leaf-bare"
    newleaf = "newleaf"
    any = "any"
